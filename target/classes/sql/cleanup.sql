-- ======================================
-- 数据库清理脚本 - 无废城市积分系统
-- ======================================
-- 警告：此脚本会删除所有现有数据，请谨慎使用！
-- 建议在执行前先备份重要数据

USE wu_fei_city;

-- ==========================================
-- 清理所有数据和结构
-- ==========================================

-- 1. 删除视图（必须先删除，因为它依赖于表）
DROP VIEW IF EXISTS user_score_ranking;

-- 2. 删除表（按依赖关系倒序删除）
-- 先删除有外键约束的子表
DROP TABLE IF EXISTS green_score_records;

-- 再删除主表
DROP TABLE IF EXISTS users;

-- 3. 可选：完全删除数据库（取消注释下面这行来执行）
-- DROP DATABASE IF EXISTS wu_fei_city;

-- ==========================================
-- 清理完成提示
-- ==========================================
SELECT '数据库清理完成！所有表和视图已删除。' AS cleanup_status;
SELECT '请运行 init.sql 重新初始化数据库结构和数据。' AS next_step; 